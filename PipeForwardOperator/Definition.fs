module Definition

let result = 0.5 |> System.Math.Sin

// this technique proves useful when working with .net libraries as it allows the compiler to infer the correct types for the functions parameters

let intList = [1;2;3]
